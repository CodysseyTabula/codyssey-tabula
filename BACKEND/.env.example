# 애플리케이션 환경 설정
APP_ENV=local
APP_DEBUG=true
APP_PORT=8000

# 데이터베이스 설정
# 로컬 개발 시: localhost 사용
# Docker Compose 사용 시: docker-compose.yml에서 DB_HOST=db로 오버라이드됨
DB_HOST=localhost
DB_PORT=5432
DB_USER=iep_user
DB_PASSWORD=iep_password
DB_NAME=iep_db

# 선택사항: 직접 데이터베이스 URL 지정 (있을 경우 아래 개별 DB_* 변수들보다 우선 적용됩니다)
# PostgreSQL 예시 (로컬):
#   postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DB_NAME>
#   예) postgresql://iep_user:iep_password@localhost:5432/iep_db

# PostgreSQL (Docker Compose 서비스명 사용) 예시:
#   postgresql://<USER>:<PASSWORD>@db:5432/<DB_NAME>
#   예) postgresql://iep_user:iep_password@db:5432/iep_db

# PostgreSQL (async 드라이버) 예시 (uvicorn + async DB 사용 시):
#   postgresql+asyncpg://<USER>:<PASSWORD>@<HOST>:<PORT>/<DB_NAME>

# SQLite (개발용, 파일 기반) 예시:
#   sqlite:///./iep_dev.db

DATABASE_URL=
