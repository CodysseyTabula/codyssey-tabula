# 애플리케이션 환경 설정
APP_ENV=local
APP_DEBUG=true
APP_PORT=8000

# 데이터베이스 설정
# 로컬 개발 시: localhost 사용
# Docker Compose 사용 시: docker-compose.yml에서 DB_HOST=db로 오버라이드됨
DB_HOST=localhost
DB_PORT=5432
DB_USER=iep_user
DB_PASSWORD=iep_password
DB_NAME=iep_db

# 선택사항: 직접 데이터베이스 URL 지정
# 설정 시 위의 개별 DB_* 변수들보다 우선 적용됨
DATABASE_URL=
